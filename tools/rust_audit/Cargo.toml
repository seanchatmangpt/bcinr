[package]
name = "rust_audit"
version = "0.1.0"
edition = "2024"
description = "Cross-checks bcinr-logic's Rust algorithm files against the Python-side algorithm mappings and each file's Branchless-Contract doc coverage."
license = "MIT OR Apache-2.0"
repository = "https://github.com/example/bcinr"
readme = "../../README.md"
keywords = ["audit", "analysis", "rust"]
categories = ["development-tools"]

[dependencies]
