I have reviewed `AGENTS.md`. According to Rule 22, "Feature and target matrix", the exact matrix of combinations that all gates must run across is:

```text
default features
no default features
all features
release profile
supported architectures
test profile where relevant
generated clean tree
```

Additionally, the rule states that passing one feature configuration does not establish repository standing, and architecture-specific instructions (like PDEP/PEXT) require an admitted target capability, a lawful fallback or typed refusal, and separate disassembly evidence.
