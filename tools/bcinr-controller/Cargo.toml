[package]
name = "bcinr-controller"
version = "26.7.25"
edition = "2021"
publish = false

license = "MIT OR Apache-2.0"
repository = "https://github.com/seanchatmangpt/bcinr"
description = "PDDL controller core: parse -> exact plan -> independent validate -> print."
readme = "../../README.md"

[dependencies]
# `mfw-planner` deliberately not enabled: only the always-available
# validate_plan/ExactClassicalProblem/evidence surface (both unconditional
# `pub mod`s in bcinr-pddl/src/lib.rs) is used here.
bcinr-pddl = { path = "../../crates/bcinr-pddl" }
