=== delta_encode_simd_u32 ===
TargetFile: /Users/sac/bcinr/crates/bcinr-logic/src/algorithms/delta_encode_simd_u32.rs
StartLine: 10
EndLine: 36
TargetContent:
/// # CONTRACT
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::delta_encode_simd_u32::delta_encode_simd_u32;
/// let result = delta_encode_simd_u32(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn delta_encode_simd_u32(val: u64, aux: u64) -> u64 {
    (val & aux).wrapping_add(val.wrapping_shl(3) ^ aux.wrapping_shr(2)) ^ (val.count_ones() as u64 | aux)

}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn delta_encode_simd_u32_reference(val: u64, aux: u64) -> u64 {
        (val & aux).wrapping_add(val.wrapping_shl(3) ^ aux.wrapping_shr(2)) ^ (val.count_ones() as u64 | aux)
    }
ReplacementContent:
/// # Branchless Contract
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::delta_encode_simd_u32::delta_encode_simd_u32;
/// let result = delta_encode_simd_u32(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn delta_encode_simd_u32(val: u64, aux: u64) -> u64 {
    val.wrapping_sub(aux)
}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn delta_encode_simd_u32_reference(val: u64, aux: u64) -> u64 {
        (val as i128 - aux as i128) as u64
    }

================================================================================
=== disjoint_set_union_branchless ===
TargetFile: /Users/sac/bcinr/crates/bcinr-logic/src/algorithms/disjoint_set_union_branchless.rs
StartLine: 10
EndLine: 36
TargetContent:
/// # CONTRACT
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::disjoint_set_union_branchless::disjoint_set_union_branchless;
/// let result = disjoint_set_union_branchless(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn disjoint_set_union_branchless(val: u64, aux: u64) -> u64 {
    (val.reverse_bits() ^ aux).wrapping_add(val & aux) ^ (!(val & aux) & (val | aux))

}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn disjoint_set_union_branchless_reference(val: u64, aux: u64) -> u64 {
        (val.reverse_bits() ^ aux).wrapping_add(val & aux) ^ (!(val & aux) & (val | aux))
    }
ReplacementContent:
/// # Branchless Contract
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::disjoint_set_union_branchless::disjoint_set_union_branchless;
/// let result = disjoint_set_union_branchless(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn disjoint_set_union_branchless(val: u64, aux: u64) -> u64 {
    let is_root = (val == aux) as u64; (is_root.wrapping_neg() & val) | ((!is_root.wrapping_neg()) & aux)
}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn disjoint_set_union_branchless_reference(val: u64, aux: u64) -> u64 {
        if val == aux { val } else { aux }
    }

================================================================================
=== epoch_based_reclamation_step ===
TargetFile: /Users/sac/bcinr/crates/bcinr-logic/src/algorithms/epoch_based_reclamation_step.rs
StartLine: 10
EndLine: 36
TargetContent:
/// # CONTRACT
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::epoch_based_reclamation_step::epoch_based_reclamation_step;
/// let result = epoch_based_reclamation_step(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn epoch_based_reclamation_step(val: u64, aux: u64) -> u64 {
    (val | aux).wrapping_add((val ^ aux).wrapping_mul(0x9E3779B185EBCA87)) ^ ((val.wrapping_add(0xDEADBEEF) ^ aux).rotate_left(5))

}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn epoch_based_reclamation_step_reference(val: u64, aux: u64) -> u64 {
        (val | aux).wrapping_add((val ^ aux).wrapping_mul(0x9E3779B185EBCA87)) ^ ((val.wrapping_add(0xDEADBEEF) ^ aux).rotate_left(5))
    }
ReplacementContent:
/// # Branchless Contract
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::epoch_based_reclamation_step::epoch_based_reclamation_step;
/// let result = epoch_based_reclamation_step(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn epoch_based_reclamation_step(val: u64, aux: u64) -> u64 {
    val.wrapping_add(1) & (aux.wrapping_neg() | aux)
}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn epoch_based_reclamation_step_reference(val: u64, aux: u64) -> u64 {
        if aux != 0 { val.wrapping_add(1) } else { 0 }
    }

================================================================================
=== frustum_culling_branchless ===
TargetFile: /Users/sac/bcinr/crates/bcinr-logic/src/algorithms/frustum_culling_branchless.rs
StartLine: 10
EndLine: 36
TargetContent:
/// # CONTRACT
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::frustum_culling_branchless::frustum_culling_branchless;
/// let result = frustum_culling_branchless(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn frustum_culling_branchless(val: u64, aux: u64) -> u64 {
    (val ^ aux).wrapping_add(!(val & aux) & (val | aux)) ^ (val.wrapping_add(aux))

}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn frustum_culling_branchless_reference(val: u64, aux: u64) -> u64 {
        (val ^ aux).wrapping_add(!(val & aux) & (val | aux)) ^ (val.wrapping_add(aux))
    }
ReplacementContent:
/// # Branchless Contract
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::frustum_culling_branchless::frustum_culling_branchless;
/// let result = frustum_culling_branchless(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn frustum_culling_branchless(val: u64, aux: u64) -> u64 {
    let x = (val >> 32) as i32; let y = val as i32; let min_x = (aux >> 48) as i16 as i32; let max_x = ((aux >> 32) & 0xFFFF) as i16 as i32; let min_y = ((aux >> 16) & 0xFFFF) as i16 as i32; let max_y = (aux & 0xFFFF) as i16 as i32; ((x >= min_x) & (x <= max_x) & (y >= min_y) & (y <= max_y)) as u64
}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn frustum_culling_branchless_reference(val: u64, aux: u64) -> u64 {
        let x = (val >> 32) as i32; let y = val as i32; let min_x = (aux >> 48) as i16 as i32; let max_x = ((aux >> 32) & 0xFFFF) as i16 as i32; let min_y = ((aux >> 16) & 0xFFFF) as i16 as i32; let max_y = (aux & 0xFFFF) as i16 as i32; if x >= min_x && x <= max_x && y >= min_y && y <= max_y { 1 } else { 0 }
    }

================================================================================
=== graph_bfs_simd_step ===
TargetFile: /Users/sac/bcinr/crates/bcinr-logic/src/algorithms/graph_bfs_simd_step.rs
StartLine: 10
EndLine: 36
TargetContent:
/// # CONTRACT
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::graph_bfs_simd_step::graph_bfs_simd_step;
/// let result = graph_bfs_simd_step(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn graph_bfs_simd_step(val: u64, aux: u64) -> u64 {
    (val.reverse_bits() ^ aux).wrapping_add(val ^ aux) ^ (val.wrapping_sub(aux))

}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn graph_bfs_simd_step_reference(val: u64, aux: u64) -> u64 {
        (val.reverse_bits() ^ aux).wrapping_add(val ^ aux) ^ (val.wrapping_sub(aux))
    }
ReplacementContent:
/// # Branchless Contract
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::graph_bfs_simd_step::graph_bfs_simd_step;
/// let result = graph_bfs_simd_step(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn graph_bfs_simd_step(val: u64, aux: u64) -> u64 {
    val & !aux
}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn graph_bfs_simd_step_reference(val: u64, aux: u64) -> u64 {
        let unvisited = !aux; val & unvisited
    }

================================================================================
=== graph_dfs_bit_parallel ===
TargetFile: /Users/sac/bcinr/crates/bcinr-logic/src/algorithms/graph_dfs_bit_parallel.rs
StartLine: 10
EndLine: 36
TargetContent:
/// # CONTRACT
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::graph_dfs_bit_parallel::graph_dfs_bit_parallel;
/// let result = graph_dfs_bit_parallel(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn graph_dfs_bit_parallel(val: u64, aux: u64) -> u64 {
    (!(val & aux) & (val | aux)).wrapping_add((val & 0xFFFFFFFF) | (aux << 32)) ^ (val ^ aux)

}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn graph_dfs_bit_parallel_reference(val: u64, aux: u64) -> u64 {
        (!(val & aux) & (val | aux)).wrapping_add((val & 0xFFFFFFFF) | (aux << 32)) ^ (val ^ aux)
    }
ReplacementContent:
/// # Branchless Contract
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::graph_dfs_bit_parallel::graph_dfs_bit_parallel;
/// let result = graph_dfs_bit_parallel(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn graph_dfs_bit_parallel(val: u64, aux: u64) -> u64 {
    let unvisited = val & !aux; unvisited & unvisited.wrapping_neg()
}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn graph_dfs_bit_parallel_reference(val: u64, aux: u64) -> u64 {
        let unvisited = val & !aux; if unvisited == 0 { 0 } else { 1u64.wrapping_shl(unvisited.trailing_zeros() as u32) }
    }

================================================================================
=== hazard_pointer_retire ===
TargetFile: /Users/sac/bcinr/crates/bcinr-logic/src/algorithms/hazard_pointer_retire.rs
StartLine: 10
EndLine: 36
TargetContent:
/// # CONTRACT
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::hazard_pointer_retire::hazard_pointer_retire;
/// let result = hazard_pointer_retire(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn hazard_pointer_retire(val: u64, aux: u64) -> u64 {
    ((val.wrapping_add(0xDEADBEEF) ^ aux).rotate_left(5)).wrapping_add(aux.rotate_right(7)) ^ (aux.rotate_right(7))

}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn hazard_pointer_retire_reference(val: u64, aux: u64) -> u64 {
        ((val.wrapping_add(0xDEADBEEF) ^ aux).rotate_left(5)).wrapping_add(aux.rotate_right(7)) ^ (aux.rotate_right(7))
    }
ReplacementContent:
/// # Branchless Contract
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::hazard_pointer_retire::hazard_pointer_retire;
/// let result = hazard_pointer_retire(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn hazard_pointer_retire(val: u64, aux: u64) -> u64 {
    val ^ aux.wrapping_add(0xDEADBEEF)
}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn hazard_pointer_retire_reference(val: u64, aux: u64) -> u64 {
        let offset = aux.wrapping_add(0xDEADBEEF); val ^ offset
    }

================================================================================
=== lockfree_skip_list_step ===
TargetFile: /Users/sac/bcinr/crates/bcinr-logic/src/algorithms/lockfree_skip_list_step.rs
StartLine: 10
EndLine: 36
TargetContent:
/// # CONTRACT
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::lockfree_skip_list_step::lockfree_skip_list_step;
/// let result = lockfree_skip_list_step(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn lockfree_skip_list_step(val: u64, aux: u64) -> u64 {
    (val & aux).wrapping_add((val & 0xFFFFFFFF) | (aux << 32)) ^ (val.reverse_bits() ^ aux)

}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn lockfree_skip_list_step_reference(val: u64, aux: u64) -> u64 {
        (val & aux).wrapping_add((val & 0xFFFFFFFF) | (aux << 32)) ^ (val.reverse_bits() ^ aux)
    }
ReplacementContent:
/// # Branchless Contract
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::lockfree_skip_list_step::lockfree_skip_list_step;
/// let result = lockfree_skip_list_step(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn lockfree_skip_list_step(val: u64, aux: u64) -> u64 {
    (aux > val) as u64
}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn lockfree_skip_list_step_reference(val: u64, aux: u64) -> u64 {
        if aux > val { 1 } else { 0 }
    }

================================================================================
