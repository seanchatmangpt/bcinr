# Auto Select and POWL Execution Tape Integration Oracle

**Owner:** `@hoare_oracle`
**Jurisdiction:** `mfw-auto-select` authoritative hot path and `bcinr-powl` scheduler bridge

This document defines the strict mathematical bounds, Hoare contracts, and proof obligations for integrating the `mfw-auto-select` deterministic polynomial with the `bcinr-powl` deterministic petri net token execution engine. The integration must strictly adhere to the BCINR Deterministic Substrate Constitution.

---

## 1. Mathematical Law and Execution Domain

The objective is to allow the branchless `mfw-auto-select` output to directly transition into a valid state on the `bcinr-powl` execution tape.
`mfw-auto-select` returns an `AutoSelectOutcome` or typed refusal.
The `bcinr-powl` tape requires a 64-bit operation mask and topological state projection.

Let $S_{out}$ be the 8-bit selected tool ID from `mfw-auto-select` ($S_{out} \in [0, 7]$).
Let $V_{valid} \in \{0, \sim0\}$ be the structural validity mask mapping from the typed outcome ($V_{valid} = \sim0$ if accepted, $0$ if refused).

The transition into the POWL token execution space must be defined as:
$$ T_{mask} = V_{valid} \land (1 \ll S_{out}) $$

This bit mask corresponds to the `fire_mask` on the POWL tape, transitioning the token deterministically to the selected tool node without any conditional branching on the hot path.

---

## 2. Hoare Contracts

### POWL Selection Bridge (`powl_bridge_select`)

**Mathematical Law:**
$$ T_{mask} = \operatorname{select}(V_{valid}, (1 \ll S_{out}), 0) $$

**Hoare Contract:**
* **Valid Input Domain:** The exact `Result<AutoSelectOutcome, AutoSelectRefusal>` yielded by `select`.
* **Output Range:** $T_{mask} \in \{0\} \cup \{2^i \mid i \in [0, 7]\}$.
* **Conservation Law:** $T_{mask}$ has exactly one bit set if and only if the `AutoSelectOutcome` is valid. Otherwise, $T_{mask} = 0$.
* **Monotonicity Law:** N/A for discrete state mapping.
* **Overflow Behavior:** $S_{out} \le 7$, so $1 \ll S_{out}$ is strictly bounded within 8 bits, completely safe within the 64-bit tape limit of POWL.
* **Invalid-Input Refusal:** If `mfw-auto-select` returns a refusal, the bridge computes $T_{mask} = 0$, deterministically preventing any token transition in the POWL scheduler without triggering early returns or panics.
* **Determinism:** Execution utilizes $CC=1$ logic. Zero conditional jumps, utilizing arithmetic mask selection mapping from the domain of typed refusals.
* **State-Mutation Boundary:** 0 heap allocations, purely algebraic translation on the stack.
* **Numeric Error Envelope:** Exact integer mathematics. $E_{abs} = 0$.

### POWL Scheduler Admission (`powl_admit_selection`)

**Mathematical Law:**
Commit $T_{mask}$ directly to the `active_mask` or `done_mask` accumulator in the POWL dynamic SWAR scheduler.
$$ active\_mask_{t+1} = active\_mask_t \lor T_{mask} $$

**Hoare Contract:**
* **Valid Input Domain:** $T_{mask}$ generated by `powl_bridge_select`.
* **Output Range:** Bitwise OR accumulator $\le 2^{64}-1$.
* **Conservation Law:** Exactly 1 additional token is routed if $V_{valid} = \sim0$, else the state is preserved bit-for-bit.
* **Monotonicity Law:** $active\_mask_{t+1} \ge active\_mask_t$ in Hamming weight.
* **Overflow Behavior:** Strictly bounded by $u64$ bitmask bounds.
* **Invalid-Input Refusal:** Handled implicitly by $T_{mask} = 0$, applying a no-op mathematically.
* **Determinism:** Bitwise OR, $CC=1$.
* **State-Mutation Boundary:** Fixed-size `PowlRunState` struct mutation, 0 heap allocations.
* **Numeric Error Envelope:** $E_{abs} = 0$.

---

## 3. Proof Obligations

To ensure rigorous integrity of the integration:

1. **Topological Object-Code Audit (@turing_machine):**
   The source audit must verify that `powl_bridge_select` and its integration with the `PowlRunState` generates assembly with zero loop backedges and zero conditional jumps (`jxx`).
2. **Refusal Conservation (@armstrong_fault):**
   Must assert that if the upstream `mfw-auto-select` pipeline yields any of its three typed refusals (e.g., `ControlStateUnadmitted`), the downstream POWL active state mask remains exactly unchanged, confirmed by generating an empty `fire_mask` ($T_{mask} = 0$).
3. **Exhaustive Mapping Matrix (@hoare_oracle):**
   Using property testing over the bounded $2^{64}$ domain, prove that every possible $S_{out} \in [0, 7]$ maps exactly to the expected $2^{S_{out}}$ bit in the execution tape, and out-of-bounds or refusal states map strictly to $0$.
