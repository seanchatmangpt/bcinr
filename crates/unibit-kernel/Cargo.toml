[package]
name = "unibit-kernel"
version = "0.1.0"
edition = "2024"

[features]
default = []

[dependencies]

[lints.rust]
unsafe_op_in_unsafe_fn = "deny"
missing_docs = "deny"

[lints.clippy]
unwrap_used = "deny"
expect_used = "deny"