=== ray_triangle_intersect_branchless ===
TargetFile: /Users/sac/bcinr/crates/bcinr-logic/src/algorithms/ray_triangle_intersect_branchless.rs
StartLine: 10
EndLine: 36
TargetContent:
/// # CONTRACT
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::ray_triangle_intersect_branchless::ray_triangle_intersect_branchless;
/// let result = ray_triangle_intersect_branchless(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn ray_triangle_intersect_branchless(val: u64, aux: u64) -> u64 {
    (!(val & aux) & (val | aux)).wrapping_add(val.wrapping_shl(3) ^ aux.wrapping_shr(2)) ^ (!(val & aux) & (val | aux))

}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn ray_triangle_intersect_branchless_reference(val: u64, aux: u64) -> u64 {
        (!(val & aux) & (val | aux)).wrapping_add(val.wrapping_shl(3) ^ aux.wrapping_shr(2)) ^ (!(val & aux) & (val | aux))
    }
ReplacementContent:
/// # Branchless Contract
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::ray_triangle_intersect_branchless::ray_triangle_intersect_branchless;
/// let result = ray_triangle_intersect_branchless(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn ray_triangle_intersect_branchless(val: u64, aux: u64) -> u64 {
    let det = val.wrapping_mul(aux); let inv_det = 1u64.wrapping_div(det | (det == 0) as u64); inv_det * (det != 0) as u64
}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn ray_triangle_intersect_branchless_reference(val: u64, aux: u64) -> u64 {
        let det = val.wrapping_mul(aux); if det == 0 { 0 } else { 1u64.wrapping_div(det) }
    }

================================================================================
=== shortest_path_bellman_ford_branchless ===
TargetFile: /Users/sac/bcinr/crates/bcinr-logic/src/algorithms/shortest_path_bellman_ford_branchless.rs
StartLine: 10
EndLine: 36
TargetContent:
/// # CONTRACT
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::shortest_path_bellman_ford_branchless::shortest_path_bellman_ford_branchless;
/// let result = shortest_path_bellman_ford_branchless(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn shortest_path_bellman_ford_branchless(val: u64, aux: u64) -> u64 {
    (val ^ aux).wrapping_add(val.rotate_left(13)) ^ ((val ^ aux).wrapping_mul(0x9E3779B185EBCA87))

}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn shortest_path_bellman_ford_branchless_reference(val: u64, aux: u64) -> u64 {
        (val ^ aux).wrapping_add(val.rotate_left(13)) ^ ((val ^ aux).wrapping_mul(0x9E3779B185EBCA87))
    }
ReplacementContent:
/// # Branchless Contract
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::shortest_path_bellman_ford_branchless::shortest_path_bellman_ford_branchless;
/// let result = shortest_path_bellman_ford_branchless(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn shortest_path_bellman_ford_branchless(val: u64, aux: u64) -> u64 {
    val.saturating_add(aux)
}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn shortest_path_bellman_ford_branchless_reference(val: u64, aux: u64) -> u64 {
        if val > u64::MAX - aux { u64::MAX } else { val + aux }
    }

================================================================================
=== topological_sort_step_branchless ===
TargetFile: /Users/sac/bcinr/crates/bcinr-logic/src/algorithms/topological_sort_step_branchless.rs
StartLine: 10
EndLine: 36
TargetContent:
/// # CONTRACT
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::topological_sort_step_branchless::topological_sort_step_branchless;
/// let result = topological_sort_step_branchless(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn topological_sort_step_branchless(val: u64, aux: u64) -> u64 {
    (!(val & aux) & (val | aux)).wrapping_add((val & 0xFFFFFFFF) | (aux << 32)) ^ (!(val & aux) & (val | aux))

}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn topological_sort_step_branchless_reference(val: u64, aux: u64) -> u64 {
        (!(val & aux) & (val | aux)).wrapping_add((val & 0xFFFFFFFF) | (aux << 32)) ^ (!(val & aux) & (val | aux))
    }
ReplacementContent:
/// # Branchless Contract
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::topological_sort_step_branchless::topological_sort_step_branchless;
/// let result = topological_sort_step_branchless(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn topological_sort_step_branchless(val: u64, aux: u64) -> u64 {
    val & !aux
}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn topological_sort_step_branchless_reference(val: u64, aux: u64) -> u64 {
        let mask = !aux; val & mask
    }

================================================================================
=== triangle_count_bitset ===
TargetFile: /Users/sac/bcinr/crates/bcinr-logic/src/algorithms/triangle_count_bitset.rs
StartLine: 10
EndLine: 36
TargetContent:
/// # CONTRACT
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::triangle_count_bitset::triangle_count_bitset;
/// let result = triangle_count_bitset(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn triangle_count_bitset(val: u64, aux: u64) -> u64 {
    ((val ^ aux).wrapping_mul(0x9E3779B185EBCA87)).wrapping_add((val ^ aux).wrapping_mul(0x9E3779B185EBCA87)) ^ (val & aux)

}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn triangle_count_bitset_reference(val: u64, aux: u64) -> u64 {
        ((val ^ aux).wrapping_mul(0x9E3779B185EBCA87)).wrapping_add((val ^ aux).wrapping_mul(0x9E3779B185EBCA87)) ^ (val & aux)
    }
ReplacementContent:
/// # Branchless Contract
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::triangle_count_bitset::triangle_count_bitset;
/// let result = triangle_count_bitset(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn triangle_count_bitset(val: u64, aux: u64) -> u64 {
    (val & aux).count_ones() as u64
}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn triangle_count_bitset_reference(val: u64, aux: u64) -> u64 {
        let mut count = 0; for i in 0..64 { if ((val & aux) & (1 << i)) != 0 { count += 1; } } count
    }

================================================================================
=== vector_cross_product_f32 ===
TargetFile: /Users/sac/bcinr/crates/bcinr-logic/src/algorithms/vector_cross_product_f32.rs
StartLine: 10
EndLine: 36
TargetContent:
/// # CONTRACT
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::vector_cross_product_f32::vector_cross_product_f32;
/// let result = vector_cross_product_f32(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn vector_cross_product_f32(val: u64, aux: u64) -> u64 {
    (val.wrapping_mul(aux.wrapping_add(1))).wrapping_add(val.reverse_bits() ^ aux) ^ (aux.rotate_right(7))

}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn vector_cross_product_f32_reference(val: u64, aux: u64) -> u64 {
        (val.wrapping_mul(aux.wrapping_add(1))).wrapping_add(val.reverse_bits() ^ aux) ^ (aux.rotate_right(7))
    }
ReplacementContent:
/// # Branchless Contract
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::vector_cross_product_f32::vector_cross_product_f32;
/// let result = vector_cross_product_f32(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn vector_cross_product_f32(val: u64, aux: u64) -> u64 {
    let ax = f32::from_bits((val & 0xFFFFFFFF) as u32); let ay = f32::from_bits((val >> 32) as u32); let bx = f32::from_bits((aux & 0xFFFFFFFF) as u32); let by = f32::from_bits((aux >> 32) as u32); let cross = ax * by - ay * bx; cross.to_bits() as u64
}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn vector_cross_product_f32_reference(val: u64, aux: u64) -> u64 {
        let ax = f32::from_bits((val & 0xFFFFFFFF) as u32); let ay = f32::from_bits((val >> 32) as u32); let bx = f32::from_bits((aux & 0xFFFFFFFF) as u32); let by = f32::from_bits((aux >> 32) as u32); let cross = (ax * by) - (ay * bx); cross.to_bits() as u64
    }

================================================================================
=== vector_dot_product_simd_f32 ===
TargetFile: /Users/sac/bcinr/crates/bcinr-logic/src/algorithms/vector_dot_product_simd_f32.rs
StartLine: 10
EndLine: 36
TargetContent:
/// # CONTRACT
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::vector_dot_product_simd_f32::vector_dot_product_simd_f32;
/// let result = vector_dot_product_simd_f32(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn vector_dot_product_simd_f32(val: u64, aux: u64) -> u64 {
    ((val.wrapping_add(0xDEADBEEF) ^ aux).rotate_left(5)).wrapping_add(aux.rotate_right(7)) ^ (val.leading_zeros() as u64 ^ aux)

}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn vector_dot_product_simd_f32_reference(val: u64, aux: u64) -> u64 {
        ((val.wrapping_add(0xDEADBEEF) ^ aux).rotate_left(5)).wrapping_add(aux.rotate_right(7)) ^ (val.leading_zeros() as u64 ^ aux)
    }
ReplacementContent:
/// # Branchless Contract
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::vector_dot_product_simd_f32::vector_dot_product_simd_f32;
/// let result = vector_dot_product_simd_f32(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn vector_dot_product_simd_f32(val: u64, aux: u64) -> u64 {
    let ax = f32::from_bits((val & 0xFFFFFFFF) as u32); let ay = f32::from_bits((val >> 32) as u32); let bx = f32::from_bits((aux & 0xFFFFFFFF) as u32); let by = f32::from_bits((aux >> 32) as u32); let dot = ax * bx + ay * by; dot.to_bits() as u64
}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn vector_dot_product_simd_f32_reference(val: u64, aux: u64) -> u64 {
        let ax = f32::from_bits((val & 0xFFFFFFFF) as u32); let ay = f32::from_bits((val >> 32) as u32); let bx = f32::from_bits((aux & 0xFFFFFFFF) as u32); let by = f32::from_bits((aux >> 32) as u32); let dot = (ax * bx) + (ay * by); dot.to_bits() as u64
    }

================================================================================
=== waitfree_queue_push ===
TargetFile: /Users/sac/bcinr/crates/bcinr-logic/src/algorithms/waitfree_queue_push.rs
StartLine: 10
EndLine: 36
TargetContent:
/// # CONTRACT
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::waitfree_queue_push::waitfree_queue_push;
/// let result = waitfree_queue_push(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn waitfree_queue_push(val: u64, aux: u64) -> u64 {
    (val.rotate_left(13)).wrapping_add(val.wrapping_sub(aux)) ^ (!(val & aux) & (val | aux))

}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn waitfree_queue_push_reference(val: u64, aux: u64) -> u64 {
        (val.rotate_left(13)).wrapping_add(val.wrapping_sub(aux)) ^ (!(val & aux) & (val | aux))
    }
ReplacementContent:
/// # Branchless Contract
/// **Ensures:** The result matches the slow but correct reference implementation for all inputs.
/// **Invariant:** Execution path is independent of input data values (Branchless).
///
/// ```rust
/// use bcinr_logic::algorithms::waitfree_queue_push::waitfree_queue_push;
/// let result = waitfree_queue_push(42, 1337);
/// assert!(result <= u64::MAX);
/// ```
#[no_mangle]
#[allow(unused_variables)]
pub fn waitfree_queue_push(val: u64, aux: u64) -> u64 {
    let tail = val & 0xFFFFFFFF; let new_tail = tail.wrapping_add(1); (val & 0xFFFFFFFF00000000) | new_tail
}

#[cfg(test)]
mod tests {
    use super::*;
    use proptest::prelude::*;
    
    // -------------------------------------------------------------------------
    // POSITIVE ORACLE: Reference implementation
    // -------------------------------------------------------------------------
    fn waitfree_queue_push_reference(val: u64, aux: u64) -> u64 {
        let tail = val & 0xFFFFFFFF; (val & 0xFFFFFFFF00000000) | (tail.wrapping_add(1))
    }

================================================================================
