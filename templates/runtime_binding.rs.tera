// GENERATED by `ggen sync` -- DO NOT EDIT.
//
// Binds each generated taxonomy to its live runtime type. Every match here has
// no `_` arm, so the graph and the executable enum cannot drift: a variant
// added to either side without the other fails to compile.
//
// This is what makes "a variant set declared in the graph is the single
// admitted fact" operationally true rather than aspirational -- without it the
// generated taxonomy is metadata that happens to agree.
{% for c in results %}
{%- if c.is_first %}
/// Classify a live `{{ c.rust_path }}` into its admitted case.
pub fn classify_{{ c.taxonomy | lower }}(value: &{{ c.rust_path }}) -> super::taxonomy::{{ c.taxonomy }} {
    match value {
{%- endif %}
        {{ c.rust_path }}::{{ c.case_name }}{{ c.pattern_suffix }} => super::taxonomy::{{ c.taxonomy }}::{{ c.case_name }},
{%- if c.is_last %}
    }
}
{%- endif %}
{%- endfor %}
