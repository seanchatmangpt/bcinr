name: CI (staged)

on:
  workflow_dispatch:

jobs:
  staged:
    name: Automatic CI withheld pending repair receipt
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Ordinary CI is intentionally withheld until the repair rail publishes an ALIVE source tree.'
